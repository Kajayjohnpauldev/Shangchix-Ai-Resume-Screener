# Minimal black & white theme — enforced regardless of the user's
# global Streamlit theme so the app always renders monochrome.
[theme]
base = "light"
primaryColor = "#111111"
backgroundColor = "#ffffff"
secondaryBackgroundColor = "#f4f4f4"
textColor = "#111111"
font = "sans serif"

[browser]
gatherUsageStats = false
